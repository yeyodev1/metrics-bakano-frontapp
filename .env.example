# En localhost no hace falta nada: el frontend apunta solo a
# http://localhost:8100/api. Ver src/config/api.ts.

# Solo para probar desde un túnel (móvil, demo a un cliente): la URL del
# túnel del BACKEND, que es distinta a la del frontend.
# VITE_API_TUNNEL_URL=https://xxxx.ngrok-free.app

# Solo para entornos desplegados. En local se ignora a propósito.
# VITE_API_BASE_URL=https://ads-bakano-clients-backapp.vercel.app/api
